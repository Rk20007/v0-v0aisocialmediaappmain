/** Lifetime starter AI generations for new users (admin can add more via starterAiBonusSlots). */
export const STARTER_FREE_AI_BASE = 2

/** Alias for older UI references */
export const FREE_IMAGES_LIMIT = STARTER_FREE_AI_BASE

/** After starter free images are used, user must upload this many reels before paid AI unlocks */
export const REELS_REQUIRED_BEFORE_PAID_AI = 5

/** Minimum coins per paid AI image (must match product rules) */
export const PAID_AI_MIN_COINS = 10

/** Profile redeem: coins → one AI generation credit */
export const REDEEM_AI_CREDIT_COST = 10
